using UnityEngine;

public enum StateZombie { 
    IDLE, // (Idle Diam Menunggu)
    PATROL, // (Patrol berkeliling area)
     CHASE, // (Chase mengejar player)
      ATTACK }  // (Attack menyerang player)